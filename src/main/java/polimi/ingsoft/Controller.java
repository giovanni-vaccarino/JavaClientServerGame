package polimi.ingsoft;

import polimi.ingsoft.model.Pattern;

public class Controller {
    void calculatePoints() {
        /*
        Pattern pattern = cartaOro.getPattern();
        int points = pattern.getMatches(tile) * cartaOro.getPoints();
        */
    }
}
